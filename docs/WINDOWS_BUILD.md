# Windows build validation

This branch validates the MSVC x64 build, automated PCM quality tests, and packaging of `SanttosCableDiagnostics.exe` on GitHub Actions Windows Server 2022.

The diagnostic executable does not install a virtual audio endpoint. The WaveRT driver remains a separate release milestone.
